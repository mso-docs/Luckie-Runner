/**
 * GameConfig - central place for tunable constants and defaults.
 */
const GameConfig = {
    camera: {
        lead: { x: 100, y: 50 },
        lerpSpeed: 0.15
    },
    level: {
        width: 3000,
        height: 600,
        spawn: { x: 100, y: 400 },
        scrollSpeed: 2
    },
    timing: {
        timeScale: 0.6,
        fps: 60
    }
};
